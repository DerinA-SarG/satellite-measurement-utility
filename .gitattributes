# Normalise to LF in the repo so shell scripts work on Linux and macOS.
* text=auto eol=lf

# Windows batch files must keep CRLF or cmd.exe mis-parses them.
*.bat text eol=crlf

*.png  binary
*.ico  binary
*.exe  binary
